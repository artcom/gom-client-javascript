# GOM Client for JavaScript

## Install dependencies

* `npm install`

## Run tests

* Open `node_modules/intern/client.html?config=tests/intern` in the browser
* Test results will be written to the console
